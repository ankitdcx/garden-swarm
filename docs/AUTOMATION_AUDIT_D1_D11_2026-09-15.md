# Automation audit: D-1 through D-11 — 15 September 2026

The user supplied an additional public-repository review and requested its findings be included in the cleanup. This reconciliation checks both repositories at public `27bb008c8916017b754e0d0f59d30bd2ffdd3f0b` and private `e58ae360e328130016ec9b5cd7659263cc5eff10`. The single Coordinator remains deliberately PAUSED after request-limit errors. No new timer or model call is authorized by this report.

## Corrected starting picture

The repository was not entirely dispatch-only: integrity, constitutional, integration and other deterministic checks run on PR/push events. Two free-provider probe/calibration workflows also retained push triggers; this repair removes those. All six workflows containing model-provider credentials now have dispatch-only triggers, one shared concurrency group and an explicit pre-provider admission block. The legacy batch workers are not qualified as single-review dispatchers and remain blocked even if someone merely changes the status label to RUNNING.

The process engine, CycleBinding, ProcessRouteReceipt, transition receipts, required gates and receipt-prefix recovery already exist in private garden-main. Its canonical section inventory and count-receipt generator also exist. Public visibility was incomplete; those implementations were not absent. The existing CIConformancePipelineReceipt is a bounded repository-check receipt, not an aggregate of independent GitHub workflow runs.

## Dispositions

| Finding | Evidence and repair | Remaining closure condition |
| --- | --- | --- |
| D-1: external clock invisible | Publish `agents/coordinator-status.json` with observed PAUSED state, both repo heads, per-lane unknown dispatch fields and no invented successes. Add freshness evaluator: missing/future/old active heartbeat cannot be called healthy. | #148: wire durable dispatch receipts to the real Coordinator. A static paused snapshot is not a continuously observed heartbeat; an observer must read it. No fallback timer was added. |
| D-2: serial prompt versus batches | The principal review loops are sequential, but they perform multiple calls and continue after failures; free calibration additionally allowed concurrency 2. Set policy concurrency to 1, share workflow concurrency, block every legacy provider worker before calls, remove residual provider push triggers, and stop free/paid/IP batches at their first failed provider result while preserving partial output. | #148/#138/#114: implement and qualify one-review-per-dispatch with durable resume and shared ownership. No claim that a concurrency key alone enforces a cross-platform call ledger. |
| D-3: state model missing | Claim disproved by private process engine and merged recovery repair #58. Public status now links the exact engine source commit. | #148: persist and consume the engine records in actual dispatch, rather than infer state ad hoc. The existing engine does not authenticate arbitrary external gate evidence. |
| D-4: $20 pools versus $1/day | Explicitly classify pools as lifetime allocations of the initial $20; all daily draws remain capped at $1. Current remaining balance is UNKNOWN/null rather than fabricated. | #75: provider reconciliation, durable atomic reservations across callers and ambiguous-charge accounting. Allocation is not a live balance. |
| D-5: competing free counts | Active shared execution limits now have one policy location. Remove README's duplicated numeric claim; label legacy lane counts as batch envelopes/diversity targets, not quota grants. Single-dispatch cap is separate from review quorum. | #75/#148: enforce shared hourly/daily quota counters in the durable dispatcher before enabling work. Blocked legacy workers cannot consume the configured quota. |
| D-6: guard does not guard itself | Add a mechanical dependency-coverage tool and publish MIGRATION_REQUIRED with seven missing paths, including the guard, its workflow, review policy inputs and constitutional FunctionContract dependencies. Prepare the exact protected migration patch. | #66/#77: activation requires the existing external/trusted-base approval verifier. Do not use this candidate's own result as approval or silently modify the protected policy. Required successful checks in branch protection remain essential. |
| D-7: incomplete config fingerprint | Include every free_swarm, specialist_free_sweep and execution_limits field plus budget scope/pools; bind the free selector/runner, specialist workflow and new boundary/dispatch checks as consumers. Derive fingerprint input from watched_fields, not a second hard-coded field list. | Closed for this configuration surface after regression tests and current-head CI. Acknowledging hashes alone still does not prove compatibility. |
| D-8: CI aggregation | Existing bounded receipt confirmed. Add read-only GitHub-run aggregation for the three named mandatory repository workflows at one exact head. Missing/in-flight/foreign/manual evidence cannot yield PASS; latest failed rerun defeats older success. Publish a required aggregate artifact. | #52/#65: configure the aggregate as an enforced gate through an appropriate branch-protection integration; extend applicability to additional conditional lanes. Current aggregate claims only the three named workflows. A merge SHA lacking a required lane remains UNKNOWN. |
| D-9: expiring artifacts | Critical provider and aggregate receipts now use missing-file errors; publish this audit's denominator and migration evidence in Git. Runtime receipt paths have explicit generating owners. The protected integrity workflow's warn-to-error change is prepared in the migration patch. | #65/#110: publish selected, exact-source-bound CI receipts through governed integration before artifact expiry and for a real release. Do not give CI unrestricted source-write authority or fabricate a release/acceptance receipt. |
| D-10: denominator missing | Execute the private producer and publish CanonicalSectionCountReceipt: TOTAL 266; Tier A 251, B 14, C 1; QUALIFIED 0; UNREVIEWED 266; exact inventory hash and source root. | #137: persist genuine qualified/stale review transitions. The output is a bootstrap enumeration, not an assertion that all historical review evidence was searched or a complete sweep passed. |
| D-11: YAML-only assertions | Extract all hourly boundary assertions into `tools/verify_review_boundaries.py`; explicit exceptions retain enforcement under Python optimization. Register check ID, termination bound, counterexample classes and tests. Add rejection of non-finite, negative and boolean costs. Wire workflow to the check and retain its receipt. | Closed for the hourly boundary block after adversarial regressions and current-head CI. Boundary PASS remains distinct from complete reviewer quorum and semantic admission. |

## Concrete protected candidate

`docs/automation-audit-2026-09-15/protected-migration.patch` updates the protected path list from the computed closure and makes the integrity conformance artifact mandatory. It is an unapplied candidate, bound to this audit's starting files. Rebase and revalidate it against the then-current protected base before approval. `gsl/CHANGE_POLICY.json` explicitly prevents self-approval; its trusted external verifier is still absent.

## Evidence and limits

- `evidence/automation-audit-2026-09-15/canonical-section-count.json`: current source denominator, not review completion.
- `evidence/automation-audit-2026-09-15/constitutional-path-coverage.json`: computed migration requirement, not authorization.
- `agents/check-definitions.json`: bounded check registration.
- Tests exercise wrong-target/paid-route/privacy/automatic-admission failures, malformed costs, stale/missing heartbeat, producer-shape mutations, CI rerun failures, and first-error batch stopping. All provider calls in tests are mocked.

No protected policy, canonical v15.5 file, authority registry, or current ProcessVersion is changed. No provider call or scheduler restart was made. Incomplete items retain their existing backlog owners rather than being closed because a document or artifact was produced.
