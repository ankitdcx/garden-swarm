name: Garden legacy free review bus (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy bus superseded
        run: |
          echo 'Scheduled Garden review is owned by review-quorum.yml.'
          echo 'This legacy workflow is intentionally manual-only so it cannot review a different target or bypass the >=3-family quorum.'
