name: Garden v15.9 wave 012-016 review

on:
  push:
    branches:
      - 'chatgpt/v159-wave1-012-016-*'
    paths:
      - 'review-inputs/v159-wave1/trigger.json'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  review:
    if: github.repository == 'ankitdcx/garden-swarm'
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        include:
          - id: P012_ALGEBRAS
            file: P012_ALGEBRAS.txt
          - id: P013_COMPARE
            file: P013_COMPARE.txt
          - id: P014_REASON
            file: P014_REASON.txt
          - id: P015_PROOF_EXPLANATION
            file: P015_PROOF_EXPLANATION.txt
          - id: P016_ORCH_SEARCH_COMPILE_TRANSITION
            file: P016_ORCH_SEARCH_COMPILE_TRANSITION.txt
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Four-family blind review
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: python tools/v159_wave_board.py "review-inputs/v159-wave1/${{ matrix.file }}"
      - name: Upload packet evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: v159-wave1-${{ matrix.id }}-${{ github.run_id }}
          path: review-results/v159-wave1/${{ matrix.id }}/
          if-no-files-found: error
          retention-days: 30
