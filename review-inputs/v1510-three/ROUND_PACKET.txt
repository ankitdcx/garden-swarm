GARDEN v15.10 — FOUR-MODEL CHEAP CLOSURE CHECK

Review ONLY the two final hardenings below. Return a blocker only if a concrete remaining defect exists.

A. CATALOGUE BOOTSTRAP / SOURCE ORDER
The Catalogue is a PRIMARY supplied artifact, not an output of canonical expansion.
BUILD_CONTEXT and SOURCE_ORDER are read only after the already-supplied Catalogue passes hash/encoding validation.
Therefore they do not participate in constructing the Catalogue and cannot form a bootstrap cycle.
Changing either record changes the Catalogue root and therefore requires selection/admission of a different successor Catalogue artifact.

SOURCE_BLOB content_sha256 binds the exact decoded predecessor UTF-8 source bytes independently of the Catalogue container encoding version.
A future Catalogue encoding migration may change container bytes but MUST preserve SOURCE_BLOB content bytes/hash or FAIL source-preservation.
SOURCE_ORDER fixes only deterministic reconstruction/display order; source position creates no semantic authority.

B. LIVE AUTHORIZATION / REVOCATION / TOCTOU
LiveAuthorizationWitness now binds:
- authority snapshot ref
- revocation root ref
- time snapshot ref
- witness ref
- exact decision nonce
- exact effect commitment
- lease expiry when lease mode is used.

Technical also REQUIRES an OwnerAuthorityEvaluation with:
- evaluator_ref
- evaluation_receipt_ref
- authority_status == AUTHORIZED
- revocation_status == NOT_REVOKED
- time_freshness_status == FRESH
- witness_authenticity == VERIFIED
- lease_status == VALID for lease mode, NOT_APPLICABLE/VALID otherwise.

Any context, effect, nonce, witness-id mismatch, missing evaluation, revoked authority, stale time, unverified witness, or invalid/missing lease returns RECHECK_REQUIRED.
Technical does NOT invent or authenticate the owner authority predicate; controlling owner/source semantics define how the evaluation receipt is authenticated and how atomic linearization / lease validity is established.
This generic gate prevents cached hash equality alone from authorizing a live effect.

CURRENT LOCAL FACTS AFTER THESE PATCHES
- final audit PASS
- exact v15.9 source reconstruction PASS
- independent checker same graph root/node count PASS
- no self-admitted laws
- no ACCEPTED_STRUCTURED
- no predecessor elision
- current release still WORKING_CANDIDATE_NOT_CANONICALLY_ADMITTED.

QUESTION
Do A and B close the remaining concrete blockers raised in the previous cheap round?
Identify ONLY a material remaining correctness, determinism, semantic-loss or authority defect.

RETURN EXACTLY:
VERDICT: APPROVE | APPROVE_WITH_PATCH | BLOCK
Then numbered MATERIAL findings only.
If none: NO MATERIAL BLOCKER FOUND.
